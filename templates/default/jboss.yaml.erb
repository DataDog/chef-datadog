# Generated by Chef, local modifications will be overwritten

<% if @init_config -%>
<%= JSON.parse(({ 'init_config' =>  @init_config }.merge({ 'instances' => @instances })).to_json).to_yaml %>

<% else -%>
init_config:
#nothing to do here

<%= JSON.parse(({ 'instances' => @instances }).to_json).to_yaml %>
<% end -%>
