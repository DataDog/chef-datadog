instances:
  <% node.datadog.haproxy.instances.each do |i| -%>
  - url:      <%= i['url'] %>
    <% if i.defined?('username') && i.defined?('password') %->
    username: <%= i['username'] %>
    password: <%= i['password'] %>
    <% end -%>
  <% end -%>

# Nothing to configure below
init_config:
